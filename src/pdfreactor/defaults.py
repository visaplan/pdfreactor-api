"""
pdfreactor.defaults: known default values
"""

default_connection = {
    'service_url': 'http://localhost:9423/service/rest',
    'license_key': None,  # optional (may be installed on the server)
    'api_key': None,      # optionally required by the service configuration
    }

default_config = {
    'disableLinks': False,
    }

